<html>
<head>
    <link href="<%= url('/styles-spaces.css') %>" rel="stylesheet" type="text/css" />
</head>
    <body>
        <header>
            <div class="logo"><img src="../logo.png" > </div>
            <div class="menu">
                <a href="/spaces">Spaces</a>
                <a href="/">Log Out</a>
            </div>
        </header>

        <div class="container">
            <div class="hero">
                <h1><%= @space.name %></h1>
                <div><img src="/house<%=@space.id%>.webp"></div>
                <div class="left-text">
                    <h2>Description</h2>
                    <p><%= @space.description %></p>
                    <h3>Availability</h2>
                    <p><%= @space.availability %> - £<%= @space.price %></p>
                </div>
            </div>
            <div class="booking-form">
            <h2>Choose your dates and book your stay</h2>
            <form action="/bookings/<%= @space.id %>" method="POST">
                <label for="start_date">Start Date</label>
                <input type="date" name="start_date">

                <label for="end_date">End Date</label>
                <input type="date" name="end_date">

                <input type="submit" value="Request Booking">
            </form>
            </div>
    </body>
</html>
